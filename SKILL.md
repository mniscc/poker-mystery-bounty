---
name: poker-mystery-bounty
description: Analyze Texas Hold'em mystery bounty tournament screenshots. Use when the user uploads poker mystery bounty screenshots or asks to calculate remaining bounty chest value, prize-pool-to-chip conversion, bounty value in chips or big blinds, current level, table stack dynamics, or bounty-stage strategy advice.
---

# 德州扑克神秘赏金分析

## 工作流

当用户上传神秘赏金赛截图并要求分析时，先提取数字，再计算，再给策略。默认使用中文回复，先给结论。

1. 从“神秘赏金/宝箱”截图提取每档金额、剩余数量、总数量。
2. 只统计剩余宝箱，计算：
   - 剩余宝箱总数
   - 剩余赏金总额
   - 单个剩余宝箱平均现金价值
3. 从赛事信息截图提取：
   - 奖池金额，注意是“奖池”而不是“赏金池”
   - 参赛总人次，优先使用“参赛人次 A/B”里的总人次 B
   - 起始记分牌，如截图显示起始记分牌为空，可用“剩余玩家 × 平均记分牌”估算当前总记分牌
   - 当前盲注级别和下一盲注级别
   - 当前平均记分牌、最大记分牌、用户名次或用户筹码
4. 计算奖金和记分牌换算：
   - 如果能取得总人次和起始记分牌：总记分牌 = 总人次 × 起始记分牌
   - 如果起始记分牌缺失：总记分牌 = 剩余玩家 × 平均记分牌，并说明这是估算
   - 1 元奖金对应记分牌 = 总记分牌 / 奖池金额
5. 换算单个剩余宝箱：
   - 宝箱记分牌价值 = 单个剩余宝箱平均现金价值 × 每元对应记分牌
   - 当前 BB = 宝箱记分牌价值 / 当前大盲
   - 如果截图有下一盲注，也计算下一盲注后的 BB
6. 结合桌况给策略建议：
   - 识别用户自己的筹码和 BB
   - 识别桌上短码、中码、大码，以及用户是否覆盖他们
   - 重点说明哪些筹码段值得猎杀，哪些筹码段要避免边缘大底池

## 读图规则

- 宝箱的 `X/Y` 表示剩余 X 个、总计 Y 个。
- 如果剩余玩家数与剩余宝箱数一致，说明数据自洽，可以明确写出。
- 如果截图时间不同步，导致剩余宝箱数与剩余玩家数只有少量差异，不要因此放弃换算。仍按宝箱列表逐档显示的剩余数量计算剩余赏金总额和平均宝箱现金价值，再用可获得的奖金池、总记分牌和盲注数据换算成大盲；同时说明数量差异可能来自客户端刷新滞后，并把平均宝箱的大盲价值明确标注为“估算值”。不要擅自把宝箱数改成剩余玩家数。
- 金额可能带逗号、小数或金币/银币图标，只保留数值。
- 盲注格式如 `2k/4k(500)`，当前大盲为第二个数字 `4k`，前注为括号内数字。
- 桌面筹码通常以 BB 显示，例如 `104.8BB`。把用户位置、短码 all-in、已下注/跟注/弃牌状态作为策略依据。
- 如果两张截图里盲注不一致，优先采用赛事信息面板的“当前级别”；如牌桌背景更实时，要说明可能差异。

## 输出格式

保持简洁，优先给用户可执行结论。

建议结构：

1. 最新结论：平均宝箱现金价值、折算记分牌、当前 BB、下一盲注 BB；存在截图时间滞后或输入数据为推算时，明确标注为估算值。
2. 宝箱统计表：金额、剩余/总数、剩余价值。
3. 奖金换算：奖池、总记分牌口径、1 元对应记分牌。
4. 桌况与策略：用户筹码、对手筹码段、猎杀建议、风险提醒。

策略语言要直接：

- 当平均宝箱价值高于短码筹码时，强调短码淘汰权很值钱。
- 用户是大筹码并覆盖短码时，建议更积极隔离短码 all-in。
- 面对不容易被淘汰的中大筹码，建议保持压迫但减少边缘大底池。
- 升盲临近时，提醒短码会更急，用户要准备接住合理范围。

## 计算脚本

可用 `scripts/calc_mystery_bounty.py` 辅助计算。脚本接收 JSON 输入并输出 JSON 结果，适合在数字已经从截图读出后使用。JSON 可通过 `--input`、`--input-file` 或标准输入传入；在 Windows 环境优先使用标准输入，避免引号转义问题。

示例：

```bash
echo '{"bounties":[{"amount":3280,"remaining":5,"total":6},{"amount":740,"remaining":12,"total":18},{"amount":240,"remaining":47,"total":64},{"amount":145.25,"remaining":244,"total":309}],"prize_pool":103603.5,"remaining_players":308,"avg_stack":133980.58,"current_big_blind":4000,"next_big_blind":5000}' | python scripts/calc_mystery_bounty.py
```
